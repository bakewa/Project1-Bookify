<h1><%= @author.name %></h1>

<%= image_tag @author.image, :class => 'feature' if @author.image.present? %>

<ul>
    <li>Name: <%= @author.name %></li> 
</ul>

<% @author.works.each do |work| %>
    <%= link_to image_tag @author.image, :class => 'thumb' if @author.image.present? %>
    <% end %>

<div class="controls">
    <%= link_to 'Edit author', edit_author_path(@author) %>
    <%= link_to 'Delete author', @author, :method => 'delete', :data => { :confirm => 'Are you sure?' } %>
<%= link_to "Back", :back%>

</div>